# URL interna da API (obrigatório em produção)
BLACKBEANS_API_URL=http://api:8000/api/v1

# PAT opcional — só para stdio/dev local ou smoke tests.
# Em produção HTTP multi-user NÃO defina: cada cliente envia
# Authorization: Bearer bb_pat_... no header do MCP.
# BLACKBEANS_PAT=

MCP_HOST=0.0.0.0
MCP_PORT=8100
