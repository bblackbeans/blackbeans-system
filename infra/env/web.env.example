NEXT_PUBLIC_API_BASE_URL=/api/v1
# Backend interno (Docker). Usado pelo proxy de /media e /api/v1.
INTERNAL_API_URL=http://api:8000
# URL pública do MCP (instruções na Conta → Tokens de API)
# NEXT_PUBLIC_MCP_URL=https://blackbeans-system-bb-system-mcp.psvs5z.easypanel.host/mcp
