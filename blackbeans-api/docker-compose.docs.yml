services:
  docs:
    image: blackbeans_api_local_docs
    container_name: blackbeans_api_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./blackbeans_api:/app/blackbeans_api:z
    ports:
      - '9000:9000'
    command: /start-docs
